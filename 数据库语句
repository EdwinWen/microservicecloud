drop database if exists cloudDB01;  

create database cloudDB01;  

use cloudDB01;  
CREATE TABLE IF NOT EXISTS `dept`(
   `deptno` INT(11) NOT NULL AUTO_INCREMENT,
   `dname` VARCHAR(100) NOT NULL,
   `db_source` VARCHAR(40) NOT NULL,
   PRIMARY KEY ( `deptno` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;

delete from  dept;
insert into dept(dname,db_source) values('软件部','db01');
insert into dept(dname,db_source) values('人事部','db01');
insert into dept(dname,db_source) values('销售部','db01');
select * from dept;

=============cloudDB02=======
drop database if exists cloudDB02;  

create database cloudDB02;  

use cloudDB02;  
CREATE TABLE IF NOT EXISTS `dept`(
   `deptno` INT(11) NOT NULL AUTO_INCREMENT,
   `dname` VARCHAR(100) NOT NULL,
   `db_source` VARCHAR(40) NOT NULL,
   PRIMARY KEY ( `deptno` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;

delete from  dept;
insert into dept(dname,db_source) values('财务部','db02');
insert into dept(dname,db_source) values('软件部','db02');
insert into dept(dname,db_source) values('大数据部','db02');
select * from dept;


=============cloudDB03=======
drop database if exists cloudDB03;  

create database cloudDB03;  

use cloudDB03;  
CREATE TABLE IF NOT EXISTS `dept`(
   `deptno` INT(11) NOT NULL AUTO_INCREMENT,
   `dname` VARCHAR(100) NOT NULL,
   `db_source` VARCHAR(40) NOT NULL,
   PRIMARY KEY ( `deptno` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;

delete from  dept;
insert into dept(dname,db_source) values('AI部','db03');
insert into dept(dname,db_source) values('手机部','db03');
insert into dept(dname,db_source) values('工单部','db03');
select * from dept;
